# ProjectYeb 调试系统使用指南

## 🎮 如何打开调试面板

### 方法1：快捷键（推荐）
- **按 Ctrl+Shift+D** - 这是主要快捷键
- **或按 F9 键** - 备用快捷键
- 调试面板会在屏幕中央弹出
- 再次按相同快捷键可以关闭调试面板

### 方法2：通过代码控制
如果您熟悉开发者工具，也可以在浏览器控制台中输入：
```javascript
window.gameSystems.debugPanel.toggle()
```

---

## 🔧 调试面板功能详解

### 主要调试功能

#### 1. 挂机倍率调整
- **位置**: 第一个输入框
- **功能**: 调整挂机收益的速度
- **范围**: 0.1倍 - 100倍
- **默认值**: 1倍（正常速度）
- **使用**: 输入数值后按回车或点击"应用修改"

#### 2. 心情值修改
- **位置**: 第二个输入框
- **功能**: 直接设置Yui的心情值
- **范围**: 0-100
- **默认值**: 40（初始不开心状态）
- **效果**: 立即改变Yui的心情等级和行为

#### 3. 经验值修改
- **位置**: 第三个输入框
- **功能**: 设置当前经验值
- **范围**: 0-无限
- **效果**: 修改后会自动检查升级

#### 4. 金币修改
- **位置**: 第四个输入框
- **功能**: 设置金币数量
- **范围**: 0-无限
- **效果**: 立即更新金币显示

#### 5. 功能按钮
- **应用修改**: 应用所有输入框的修改
- **下一行为**: 强制Yui切换到下一个随机行为

---

## ⌨️ 调试快捷键

### 主要快捷键
| 快捷键 | 功能 | 说明 |
|--------|------|------|
| **Ctrl+Shift+D** | 切换调试面板 | 打开/关闭调试窗口 |
| **F9** | 切换调试面板 | 备用快捷键 |
| **Ctrl+Shift+T** | 强制触发对话 | 立即开始随机对话（需调试面板打开） |
| **Ctrl+Shift+R** | 重置游戏 | 清除所有数据重新开始（需调试面板打开） |

### 使用示例
1. 按 **Ctrl+Shift+D** 打开调试面板
2. 将挂机倍率改为 **10**（10倍速度）
3. 将心情值改为 **80**（开心状态）
4. 点击 **"应用修改"** 按钮
5. 观察游戏变化

---

## 🎯 调试技巧

### 快速测试心情系统
1. 设置心情值为 **20** → 观察"讨厌"状态的行为
2. 设置心情值为 **60** → 观察"平常"状态的行为  
3. 设置心情值为 **90** → 观察"开心"状态的行为
4. 设置心情值为 **100** → 触发"超级开心"状态

### 快速测试等级系统
1. 设置经验值为 **100** → 立即升到2级
2. 设置经验值为 **1000** → 立即升到5级
3. 观察升级奖励和通知

### 测试对话系统
1. 按 **F11** 强制触发对话
2. 测试不同心情状态下的对话内容
3. 测试对话选项的效果

---

## 🔍 调试信息查看

### 控制台信息
打开浏览器开发者工具（F12），查看控制台输出：
- 系统初始化状态
- 心情变化日志
- 行为切换记录
- 对话触发信息

### 游戏状态查看
在控制台中输入：
```javascript
// 查看游戏基本信息
console.log(window.projectYebGame.getGameInfo())

// 查看调试信息
console.log(window.gameSystems.debugPanel.getDebugInfo())

// 查看当前心情信息
console.log(window.gameSystems.moodSystem.getMoodInfo())

// 查看当前行为信息
console.log(window.gameSystems.behaviorSystem.getCurrentBehaviorInfo())
```

---

## 🛠️ 高级调试功能

### 自定义调试命令
在控制台中可以添加自定义调试命令：
```javascript
// 添加自定义命令
window.gameSystems.debugPanel.addDebugCommand('test', function() {
    console.log('自定义调试命令执行成功！')
})

// 执行自定义命令
window.gameSystems.debugPanel.executeDebugCommand('test')
```

### 强制功能测试
```javascript
// 强制关闭当前对话
window.gameSystems.dialogueSystem.forceCloseDialogue()

// 强制显示通知
window.gameSystems.notificationSystem.showNotification('测试通知', 'info')

// 强制触发特殊事件
window.projectYebGame.triggerSpecialEvent('mood_max')
```

---

## ⚠️ 注意事项

1. **数据保存**: 调试修改会自动保存到本地存储
2. **重置警告**: 使用F10重置会清除所有游戏数据
3. **倍率限制**: 挂机倍率过高可能导致数值溢出
4. **测试模式**: 当前版本每次刷新会重置数据（可在配置中关闭）

---

## 🎮 推荐测试流程

### 基础功能测试
1. 打开游戏，等待挂机收益
2. 按F12打开调试面板
3. 测试各种数值修改
4. 测试对话系统
5. 测试通知系统

### 系统集成测试
1. 设置心情80，触发开心状态对话
2. 设置高倍率快速升级
3. 测试心情满值特殊事件
4. 测试等级达到5级事件

---

## 📞 技术支持

如果调试功能出现问题：
1. 检查浏览器控制台错误信息
2. 确认所有JavaScript文件正确加载
3. 尝试刷新页面重新初始化
4. 检查本地存储是否被禁用

**调试系统是开发阶段的重要工具，请合理使用以测试游戏功能！**
